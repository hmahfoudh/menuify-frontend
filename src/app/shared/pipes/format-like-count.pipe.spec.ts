import { FormatLikeCountPipe } from './format-like-count.pipe';

describe('FormatLikeCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLikeCountPipe();
    expect(pipe).toBeTruthy();
  });
});
